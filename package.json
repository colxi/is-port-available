{
  "name": "is-port-available",
  "version": "0.1.0",
  "description": "Check if the requested port is available or instead used",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "colxi <colxi.kl@gmail.com> (http://colxi.info)",
  "license": "GPL",
  "dependencies": {
  }
}
